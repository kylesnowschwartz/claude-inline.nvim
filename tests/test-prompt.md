Create two parallel sub-agents. Each should independently verify which is the most starred repo on github, using gh.

Create two parallel sub-agents. Each should independently make a simple sequence diagram of claude-inline's conversation flow
